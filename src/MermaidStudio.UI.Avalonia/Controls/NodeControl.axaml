<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="MermaidStudio.UI.Avalonia.Controls.NodeControl"
             x:CompileBindings="False"
             Width="160"
             Height="70">

	<Grid x:Name="RootGrid">
		<!-- Rectangle / Rounded -->
		<Border x:Name="RootBorder"
                Background="#2D2D30"
                BorderBrush="#6A6A6A"
                BorderThickness="2"
                CornerRadius="0"
                IsVisible="True"/>

		<!-- Decision -->
		<Polygon x:Name="DecisionShape"
                 Fill="#2D2D30"
                 Stroke="#6A6A6A"
                 StrokeThickness="2"
                 IsVisible="False"/>

		<!-- Circle -->
		<Ellipse x:Name="CircleShape"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Fill="#2D2D30"
                 Stroke="#6A6A6A"
                 StrokeThickness="2"
                 IsVisible="False"/>

		<!-- Label -->
		<TextBlock x:Name="LabelText"
                   Text="{Binding Label}"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextAlignment="Center"
                   TextWrapping="Wrap"
                   MaxWidth="180"
                   Margin="14,10,14,10"/>

		<!-- Port droit actif -->
		<Ellipse x:Name="RightPort"
                 Width="8"
                 Height="8"
                 Fill="#AAAAAA"
                 HorizontalAlignment="Right"
                 VerticalAlignment="Center"
                 Margin="0,0,-4,0"
                 Cursor="Cross"
                 PointerPressed="OnRightPortPressed"
                 PointerMoved="OnRightPortMoved"
                 PointerReleased="OnRightPortReleased"/>
	</Grid>
</UserControl>
